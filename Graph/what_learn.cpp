/*
dfs
bfs
cycle in graph(bfs)
cycle in graph(dfs)
bipartite(colouring) bfs 
bipartite(colouring) dfs
shortest path with unit wt(bfs)
shortest path with unit wt(dfs)
0-1 bfs 
Dijkstra's algorithm (bfs)
Dijkstra's algorithm (dfs)
Topo sort(bfs)
Topo sort(dfs)
kahn's algo
minimum spaning tree
prims algorithm(brute approch)
prims algorithm(efficient approch)
*/

